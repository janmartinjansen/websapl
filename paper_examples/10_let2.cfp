// IFL 2016 Paper - Section 5.2 (Topological Let Sorting)
// Dependency ordering: b depends on a, but is declared first.
// The compiler automatically computes the topological ordering.

start = f 3 4

f x y =
  let b = a * 2,
      a = x + 1 in
  a + b
